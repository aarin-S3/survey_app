<template>
  <q-item clickable active-class="my-active" :to="{name: to}" style="color: #455a64;">
    <q-item-section avatar>
      <q-icon :name="icon" class="icon-drawer"/>
    </q-item-section>
    <q-item-section >
      <q-item-label>{{ text }}</q-item-label>
      <q-item-label caption v-if="description" style="font-size:0.7rem">{{ description }}</q-item-label>
    </q-item-section>
    <q-item-label class="q-badge" :class="badge ==='10.62%' ? 'q-badge-danger': ''" v-if="badge">{{ badge }}</q-item-label>
  </q-item>
</template>
<script lang="ts">
import { defineComponent } from 'vue'
let drawer = false
export default defineComponent({
  props: {
    icon: {
      type: String,
      default: 'navigate_next'
    },
    text: {
      type: String,
      default: 'menu'
    },
    to: {
      type: String,
      default: ''
    },
    description: {
      type: String,
      default: ''
    },
    badge:{
      type:String,
      default : ""
    }
  }
})
</script>

<style scoped>
.q-item:hover {
  color: #027be3 !important;
}
.q-badge{
  padding:3px 6px;
  border-radius: 5px;
  background-color: #9c27b0;
  color: white;
  height: fit-content;
}

.q-badge-danger{
  background-color: rgb(203, 0, 0);
  margin-top: 7px;
}

</style>
